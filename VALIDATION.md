# 验证记录

日期：2026-09-19。版本：0.2.0。

## 已验证

- Node.js 22.23.2，Pi Coding Agent 0.85.1，TypeSafe SDK 0.6.0。
- `npm run typecheck` 通过。
- `npm test`：58 项通过，0 失败，0 跳过。
- 使用 Pi 的真实 TypeScript 扩展加载器加载入口，并通过真实 ExtensionRunner 分发事件；不是仅用自定义回调模拟 Pi。
- 在隔离的工作目录及 Pi 配置目录中执行了本地 `pi install -l`，成功登记包，没有修改用户的全局 Pi 设置。
- `npm pack --dry-run` 成功；发布清单仅包含源码、说明、示例配置、许可证及包配置，不包含凭据、node_modules 或测试材料。

## 测试覆盖

风险动作的确认／拦截、只读操作与误报样本、无交互界面处理、确认窗口异常、单次授权不复用、观察模式、规则提醒、Jev SDK 请求格式和响应验证、超时不重试、取消与会话关闭、输出及元数据脱敏、输出扫描失败和持久化失败、重复失败提醒、验证失败／成功、并行修改与检查、会话恢复及新请求提醒范围。

新增覆盖：四个自定义规则时机、非法时机／动作组合、重复 ID 与字段拼写错误、路径边界、本地匹配、批量语义问题及请求脱敏、无 key／无效响应／过长上下文的失败策略、自带规则开关、自定义规则优先拦截、输出隐藏连同元数据、原生配置向导回调、配置保存与重新加载、无效编辑保持旧配置、启动配置错误拦截及修复恢复。

原生向导使用真实 ExtensionRunner 的命令上下文，UI 输入由测试提供；不等同于真人终端交互。

## 尚未验证

当前环境未配置 `TYPESAFE_API_KEY`。所有 Jev 返回使用可控测试传输或判定器，尚未测量真实模型准确率、误报率、延迟或费用；也未做真人交互终端的完整演示。默认阈值没有真实数据校准。

DSH 适配不在此版本中；未发布到 npm，未安装进用户的全局 Pi 配置。输出脱敏不覆盖更早的流式显示、其他插件日志或已经发生的外传。
